# Core dependencies for Wildfire Damage Detection
# Note: UV is the recommended way to install. Use this file only if needed.

flask>=3.0.0
torch>=2.0.0
torchvision>=0.15.0
terratorch>=0.1.0
rasterio>=1.3.0
numpy>=1.24.0
pillow>=10.0.0
transformers>=4.30.0
timm>=0.9.0
einops>=0.7.0
scipy>=1.11.0
